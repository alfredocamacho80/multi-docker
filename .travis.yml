sudo: required
language: generic

services:
  - docker

before_install:
  - docker built -t alfredocamacho80/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run alfredocamacho80/react-test npm test -- --coverage

after_success:
  - docker built -t alfredocamacho80/multi-client ./client
  - docker built -t alfredocamacho80/multi-nginx ./nginx
  - docker built -t alfredocamacho80/multi-server ./server
  - docker built -t alfredocamacho80/multi-worker ./worker
#Proceso de login a docker hub
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
#Cargar imagenes a docker hub
  - docker push alfredocamacho80/multi-client
  - docker push alfredocamacho80/multi-nginx
  - docker push alfredocamacho80/multi-server
  - docker push alfredocamacho80/multi-worker